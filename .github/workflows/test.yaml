name: Test

on:
  workflow_call:

jobs:
  test:
    strategy:
      matrix:
        node-version: [22, 24]

    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - run:
          npm run test